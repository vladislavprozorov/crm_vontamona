# 🎯 CRM Система - Full Stack приложение

[![CI/CD Pipeline](https://github.com/vladislavprozorov/crm_vontamona/actions/workflows/ci.yml/badge.svg)](https://github.com/vladislavprozorov/crm_vontamona/actions/workflows/ci.yml)

Полнофункциональная CRM система для управления клиентами с Next.js фронтендом и NestJS бэкендом.

## 📦 Структура проекта

```
crm-test/
├── server/          # NestJS Backend (порт 3001)
├── client/          # Next.js Frontend (порт 3000)
└── docker-compose.yml
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

**Бэкенд:**

```bash
cd server
pnpm install
```

**Фронтенд:**

```bash
cd client
npm install
```

### 2. Настройка базы данных

**Запустить PostgreSQL через Docker:**

```bash
docker compose up -d
```

**Настроить переменные окружения:**

`server/.env`:

```env
DB_USER=root
DB_PASSWORD=123456
DB_HOST=localhost
DB_PORT=5433
DB_NAME=nestjs-db
DB_SYNCHRONIZE=false
```

`client/.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:3001
```

### 3. Запуск приложений

#### Вариант 1: Docker Compose (Рекомендуется) 🐳

Запустить весь стек (PostgreSQL + Backend + Frontend) одной командой:

```bash
docker compose up --build
```

Сервисы будут доступны:
- 🎨 Frontend: http://localhost:3000
- 🔧 Backend API: http://localhost:3001
- 🗄️ PostgreSQL: localhost:5433

Остановить:
```bash
docker compose down
```

Остановить и удалить volumes:
```bash
docker compose down -v
```

#### Вариант 2: Локальная разработка

**Терминал 1 - Бэкенд:**

```bash
cd server
pnpm start:dev
```

Бэкенд доступен: http://localhost:3001

**Терминал 2 - Фронтенд:**

```bash
cd client
npm run dev
```

Фронтенд доступен: http://localhost:3000

## ✨ Функциональность

### Клиенты (Clients)

- ✅ Создание нового клиента
- ✅ Просмотр списка всех клиентов
- ✅ Просмотр детальной информации
- ✅ Редактирование данных клиента
- ✅ Удаление клиента
- ✅ Валидация данных

## 🧪 Тестирование

### Бэкенд тесты

**Unit тесты (22 теста):**

```bash
cd server
pnpm test
```

**E2E тесты (18 тестов):**

```bash
pnpm test:e2e
```

**Покрытие кода:**

```bash
pnpm test:cov
```

**Результаты:**

- ClientsController: 100% покрытие
- ClientsService: 100% покрытие
- Всего: 40 тестов (22 unit + 18 E2E)

### Фронтенд тесты (15 тестов)

**Запустить все тесты:**

```bash
cd client
npm test
```

**Режим watch:**

```bash
npm run test:watch
```

**С UI интерфейсом:**

```bash
npm run test:ui
```

**Покрытие кода:**

```bash
npm run test:coverage
```

**Что покрыто:**
- ✅ ClientCard компонент (4 теста)
- ✅ API клиент (6 тестов)
- ✅ Утилиты (5 тестов)

## 📚 Технологии

### Backend

- **NestJS** - фреймворк
- **TypeORM** - ORM для работы с БД
- **PostgreSQL** - база данных
- **Jest** - тестирование
- **Supertest** - E2E тестирование
- **class-validator** - валидация
- **class-transformer** - трансформация данных

### Frontend

- **Next.js 16** (App Router)
- **React 19**
- **TypeScript**
- **Tailwind CSS v4**
- **shadcn/ui** - UI компоненты
- **TanStack Query** - управление состоянием
- **Lucide React** - иконки
- **date-fns** - работа с датами
- **Vitest** - тестирование (15 тестов)

### DevOps

- **Docker** - контейнеризация
- **Docker Compose** - оркестрация (3 сервиса: PostgreSQL, Backend, Frontend)
- **GitHub Actions** - CI/CD (4 jobs: 2 для backend, 2 для frontend)
- **CORS** - кросс-доменные запросы

## 🔗 API Endpoints

### Clients

```
GET    /clients          # Получить всех клиентов
GET    /clients/:id      # Получить клиента по ID
POST   /clients          # Создать нового клиента
PUT    /clients/:id      # Обновить клиента
DELETE /clients/:id      # Удалить клиента
```

### Примеры запросов

**Создание клиента:**

```json
POST /clients
{
  "fullName": "Иван Иванов",
  "email": "ivan@example.com",
  "description": "Важный клиент компании"
}
```

**Ответ:**

```json
{
  "id": "uuid",
  "fullName": "Иван Иванов",
  "email": "ivan@example.com",
  "description": "Важный клиент компании",
  "createdAt": "2026-02-15T20:00:00.000Z"
}
```

## 📱 Страницы фронтенда

- `/` - Главная страница
- `/clients` - Список клиентов
- `/clients/new` - Создание нового клиента
- `/clients/[id]` - Детали клиента
- `/clients/[id]/edit` - Редактирование клиента

## 🎨 Особенности UI

- 🎨 Современный дизайн с Tailwind CSS
- 📱 Адаптивная верстка
- � Дизайн в стиле Яндекс
- 🌈 Яркая желтая тема (#FC0)
- ✨ Плавные анимации
- 🔔 Подтверждение при удалении
- ⚡ Быстрая валидация форм
- 🔄 Автоматическое обновление данных (TanStack Query)

## 🔧 Разработка

### Структура бэкенда

```
server/
├── src/
│   ├── clients/
│   │   ├── clients.controller.ts
│   │   ├── clients.service.ts
│   │   ├── clients.module.ts
│   │   ├── dto/
│   │   └── entity/
│   ├── config/
│   └── main.ts
├── test/
└── coverage/
```

### Структура фронтенда

```
client/
├── app/
│   ├── page.tsx
│   └── clients/
├── components/
│   ├── ClientCard.tsx
│   ├── ClientForm.tsx
│   ├── ui/           # shadcn/ui компоненты
│   └── providers/
├── lib/
│   └── api/
└── types/
```

## 🚦 CI/CD Pipeline

Проект использует **GitHub Actions** для автоматической сборки и тестирования.

### Pipeline включает 4 jobs:

#### Backend Jobs:
1. **backend-build** - Сборка бэкенда
   - Установка зависимостей через pnpm
   - Компиляция TypeScript
   - Создание production build

2. **backend-test** - Тестирование бэкенда
   - Запуск PostgreSQL в Docker
   - Unit тесты (22 теста)
   - E2E тесты (18 тестов)
   - Генерация coverage отчета

#### Frontend Jobs:
3. **frontend-build** - Сборка фронтенда
   - Установка зависимостей через npm
   - Next.js production build
   - Оптимизация ассетов

4. **frontend-test** - Тестирование фронтенда
   - ESLint проверка кода
   - TypeScript type checking
   - Vitest тесты (15 тестов)
   - Генерация coverage отчета

### Запуск CI:

Pipeline запускается автоматически при:
- Push в ветки `main` или `develop`
- Создании Pull Request

### Просмотр результатов:

```
GitHub → Actions → CI/CD Pipeline
```

## 🐛 Устранение неполадок

### Проблема: База данных не подключается

**Решение:**

```bash
# Проверить статус контейнера
docker ps

# Перезапустить
docker compose restart
```

### Проблема: CORS ошибка

**Решение:**

- Проверьте, что бэкенд запущен на порту 3001
- Проверьте настройки в `server/src/main.ts`
- Проверьте `.env.local` во фронтенде

### Проблема: Фронтенд не видит данные

**Решение:**

- Убедитесь, что оба сервера запущены
- Проверьте URL в `.env.local`
- Откройте DevTools и проверьте Network tab

## ✅ Требования лабораторной работы

### Все требования выполнены:

#### ✅ 1. Изучение принципов DevOps
- Применены на практике: контейнеризация, CI/CD, автоматизация тестирования

#### ✅ 2. Установка и настройка Git
- Репозиторий: [vladislavprozorov/crm_vontamona](https://github.com/vladislavprozorov/crm_vontamona)
- Коммиты с осмысленными сообщениями

#### ✅ 3. RESTful сервис с 4 CRUD операциями
- **GET** `/clients` - получить всех клиентов
- **GET** `/clients/:id` - получить клиента по ID
- **POST** `/clients` - создать клиента
- **PUT** `/clients/:id` - обновить клиента
- **DELETE** `/clients/:id` - удалить клиента

#### ✅ 4. Клиентское веб приложение
- Next.js 16 + React 19
- TypeScript для типобезопасности
- Tailwind CSS v4 для стилизации
- shadcn/ui компоненты
- TanStack Query для управления состоянием

#### ✅ 5. Наличие базы данных
- PostgreSQL 16
- Docker контейнер
- TypeORM для миграций и сущностей
- Порт 5433 (хост) → 5432 (контейнер)

#### ✅ 6. Модульные тесты серверного приложения
- **22 unit теста** - моки репозиториев и сервисов
- **18 E2E теста** - интеграционные с реальной БД
- **Итого: 40 тестов**
- Coverage: 100% основных модулей
- Framework: Jest

#### ✅ 7. Модульные тесты клиентского приложения
- **4 теста** ClientCard компонента
- **6 тестов** API client модуля
- **5 тестов** utility функций
- **Итого: 15 тестов**
- Framework: Vitest + Testing Library

#### ✅ 8. REST коммуникация
- CORS настроен между фронтом и беком
- API endpoint: `http://localhost:3001/clients`
- Обработка ошибок на клиенте
- Автоматическое обновление после изменений

#### ✅ 9. Размещение в системе контроля версий
- GitHub: vladislavprozorov/crm_vontamona
- Все изменения коммитятся
- .gitignore настроен правильно

#### ✅ 10. CI/CD с минимум 4 jobs
**Backend (2 jobs):**
1. `backend-build` - установка зависимостей, компиляция TS
2. `backend-test` - unit + E2E тесты с PostgreSQL в Docker

**Frontend (2 jobs):**
3. `frontend-build` - установка зависимостей, Next.js build
4. `frontend-test` - lint, typecheck, Vitest тесты (15)

**Итого: 4 jobs** ✅

### 🎯 Итоговая статистика:

| Метрика | Значение |
|---------|----------|
| **Всего тестов** | 55 (40 backend + 15 frontend) |
| **Backend coverage** | ~100% |
| **CI/CD jobs** | 4 (требование выполнено) |
| **Docker services** | 3 (postgres + backend + frontend) |
| **API endpoints** | 5 (CRUD + список) |
| **Технологии** | 15+ (NestJS, Next.js, PostgreSQL, Docker, etc.) |

### UI/UX Features:

- 🎨 Дизайн в стиле Яндекс
- 🟨 Фирменный желтый цвет (#FC0)
- 📦 shadcn/ui компоненты
- 🔄 Автоматическое обновление списка (TanStack Query)
- ✨ Lucide React иконки
- 📱 Адаптивная верстка

## 📈 Следующие шаги (опционально)

- [x] Добавить shadcn/ui компоненты
- [x] Настроить CI/CD с GitHub Actions
- [x] Автоматическое обновление после CRUD операций
- [x] Добавить unit тесты для фронтенда (15 тестов)
- [x] Docker Compose для полного стека
- [ ] Добавить пагинацию списка клиентов
- [ ] Добавить поиск по клиентам
- [ ] Добавить модуль Requests (Заявки)
- [ ] Deploy на Vercel/Railway

## 👥 Авторы

Vladislav Prozorov

## 📝 Лицензия

MIT
